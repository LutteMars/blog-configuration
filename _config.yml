# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html
## Source: https://github.com/hexojs/hexo/

# Site
title: LutteMars的博客
subtitle: 愿我来世得菩提时，身如琉璃，内外明彻，净无瑕秽。 
description:
author: LutteMars
language:
github: "https://github.com/LutteMars/"
email:  <livjunjie@163.com>
weibo: "http://weibo.com/luttemars" 
timezone:
keywords: 

# URL
## If your site is put in a subdirectory, set url as 'http://yoursite.com/child' and root as '/child/'
url: https://luttemars.github.io
root: /
permalink: :year/:month/:day/:title/
permalink_defaults:

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Category
category_generator:
	per_page: 10

# Writing
new_post_name: :title.md # File name of new posts
default_layout: post
titlecase: false # Transform title into titlecase
external_link: false # Open external links in new tab
filename_case: 0
render_drafts: false
post_asset_folder: false
relative_link: false

excerpt_link: 

highlight:
  enable: true
  line_number: true
  auto_detect: true
  tab_replace: ''

# Category & Tag
default_category: categorized
category_map: 
tag_map:

# Archives
## 2: Enable pagination
## 1: Disbale pagination
## 0: All Disable
archive: 2
category: 2
tag: 2 

# Server
## Hexo uses Connect as a server
port: 4000
server_ip: localhost
logger: false
logger_format: dev

# Date / Time format
## Hexo uses Moment.js to parse and display date
## You can customize the date format as defined in
## http://momentjs.com/docs/#/displaying/format/
date_format: YYYY-MM-DD
time_format: HH:mm:ss

# Pagination
## Set per_page to 0 to disable pagination
per_page: 8
pagination_dir: page

# Extensions
## Plugins: https://hexo.io/plugins/
## Themes: https://hexo.io/themes/
## plugins:
#	- hexo-generator-feed
#	- hexo-generator-baidu-sitemap
# 	- hexo-generator-sitemap

theme: yilia
exclude_generator:

# Sitemap
sitemap:
    path: sitemap.xml
baidusitemap:
    path: baidusitemap.xml

feed:
    type: atom
    path: atom.xml
    limit: 100
    hub:

# Deployment
## Docs: https://hexo.io/docs/deployment.html
#deploy:
 # type: git
 # repository: git@github.com:LutteMars/LutteMars.github.io.git
 # branch: master

# Comment
## 鉴于多说官方决定在2017年6月1日关闭多说，
## 所以评论系统由多少向disqus迁移
#comment_provider: disqus

# Disqus
#disqus_shortname: https-luttemars-github-io

#畅言
changyan_appid: cysWNgenz
changyan_conf: prod_4dbf7371eb6398cc3db686d73dbafc4c

#Content
jsonContent:
    meta: false
    pages: false
    posts:
      title: true
      date: true
      path: true
      text: true
      raw: false
      content: false
      slug: false
      updated: false
      comments: false
      link: false
      permalink: false
      excerpt: false
      categories: true
      tags: true
      archives: true


# Hexo-all-minifier

html_minifier:
  enable: true
  ignore_error: false
  exclude:

## **enable** - Enable the plugin. Defaults to `true`.
## **ignore_error** - Ignore the error occurred on parsing html
## **exclude**: Exclude files

css_minifier:
  enable: true
  exclude: 
    - '*.min.css'

## **enable** - Enable the plugin. Defaults to `true`.
## **exclude**: Exclude files

js_minifier:
  enable: true
  mangle: true
  output:
  compress:
  exclude: 
    - '*.min.js'

## **enable** - Enable the plugin. Defaults to `true`.
## **mangle**: Mangle file names
## **output**: Output options
## **compress**: Compress options
## **exclude**: Exclude files

image_minifier:
  enable: true
  interlaced: false
  multipass: false
  optimizationLevel: 2
  pngquant: false
  progressive: false

## **enable** - Enable the plugin. Defaults to `true`.
## **interlaced** - Interlace gif for progressive rendering. Defaults to `false`.
## **multipass** - Optimize svg multiple times until it’s fully optimized. Defaults to `false`.
## **optimizationLevel** - Select an optimization level between 0 and 7. Defaults to `2`.
## **pngquant** - Enable [imagemin-pngquant](https://github.com/imagemin/imagemin-pngquant) plugin. Defaults to `false`.
## **progressive** - Lossless conversion to progressive. Defaults to `false`.
## **exclude** - Exclude specific types of image files, the input value could be `gif`,`jpg`, `png`, or `svg`. Default to null.
